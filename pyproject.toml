[project]
name = "data-analysis-template"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pyprojroot",
    "numpy",
    "ipykernel"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["config*", "src*"]